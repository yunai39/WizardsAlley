{% extends '::base.html.twig' %}

{% block body -%}
    <h1>SmallPublication list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Content</th>
                <th>Datepublication</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('user_small_publication_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.content }}</td>
                <td>{% if entity.created_at %}{{ entity.created_at|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('user_small_publication_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('user_small_publication_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('user_small_publication_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
