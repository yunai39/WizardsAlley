
    {% for pageFav in pages %}
        <div class="page col-xs-12">
            <div class="row search-pages-container">
                <div class="col-md-4 img-wrapper">
                    <div class="imgSquare">
                        <div class="imgSquareContainer">
                            <img src="{{ assets('/uploads/page/profile/' ~ pageFav.page.pathProfile)| imagine_filter('small_thumb') }}"/>
                        </div>
                    </div>
                </div>
                <div class="col-md-8">
                    <h4>
                        <a href="{{ path('page_show' , {'id_page': pageFav.page.id}) }}">{{ pageFav.page.name }}</a>
                    </h4>
                </div>
            </div>
        </div>
    {% endfor %}
