{% for publication in publications %}

    {% include 'includes/publication/bigPublication.html.twig' with {
    'publication_path': 'uploads/page/profile/' ~ publication.page.pathProfile,
    'publication_user_id': publication.user.id,
    'publication_user_path_profile': publication.page.pathProfile,
    'publication_user_username': publication.user.username,
    'publication_created_at': publication.createdAt,
    'publication_id': publication.id,
    'publication_type': 'page_publication',
    'publication_title': publication.title,
    'publication_name': publication.page.name,
    'publication_page_id': publication.page.id,
    'publication_content': publication.smallContent
    } %}
{% endfor %}