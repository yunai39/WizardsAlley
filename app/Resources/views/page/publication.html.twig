{% for publication in publications %}

    {% include 'includes/publication/bigPublication.html.twig' with {
        'publication_path': 'uploads/page/profile/' ~ publication.path_profile,
        'publication_user_id': publication.user_id,
        'publication_user_path_profile': publication.path_profile,
        'publication_user_username': publication.user_id,
        'publication_created_at': publication.created_at,
        'publication_id': publication.id,
        'publication_type': 'page_publication',
        'publication_title': publication.title,
        'publication_name': publication.name,
        'publication_page_id': publication.page_id,
        'publication_content': publication.small_content
    } %}
{% endfor %}