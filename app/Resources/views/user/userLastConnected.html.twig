<div class="row">
    {% for user in users %}
        <div class="user-last-connecter-wrapper col-xs-6 col-md-4 col-lg-3" >

            {% include 'includes/viewSmall.html.twig' with
            {
            'path': user.getWebPathProfile(),
            'link':  path('wizardalley_user_wall' , {'id': user.id}),
            "title": user.username
            } %}
        </div>
    {% endfor %}
</div>

