{% extends '::base.html.twig' %}

{% block body -%}
    <h1>PageCategory edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_list_page', {'tableName': 'category'}) }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
