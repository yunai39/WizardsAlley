{% extends '::base.html.twig' %}

{% block body -%}
    <h1>PageCategory creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_list_page', {'tableName': 'category'}) }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
